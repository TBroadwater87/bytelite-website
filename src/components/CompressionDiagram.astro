---
// CompressionDiagram.astro
---

<section class="diagram-section">
  <h2>Compression Pipeline</h2>
  <div class="pipeline">
    <div class="stage">
      <div class="stage-icon">ðŸ“</div>
      <div class="stage-label">Input Data</div>
      <div class="stage-detail">Any Size</div>
    </div>
    
    <div class="arrow">â†’</div>
    
    <div class="stage">
      <div class="stage-icon">ðŸ”„</div>
      <div class="stage-label">Recursive Transform</div>
      <div class="stage-detail">Rounds 1-N</div>
    </div>
    
    <div class="arrow">â†’</div>
    
    <div class="stage">
      <div class="stage-icon">ðŸ’Ž</div>
      <div class="stage-label">Output</div>
      <div class="stage-detail">â‰¤18 Bytes</div>
    </div>
  </div>
  
  <div class="diagram-details">
    <div class="detail-box">
      <h4>Round 1-3: Aggregation</h4>
      <p>8-bit â†’ 16-bit â†’ 32-bit â†’ 64-bit</p>
    </div>
    <div class="detail-box">
      <h4>Round 0: SDD Encoding</h4>
      <p>64-bit values â†’ 8-bit dictionary indices</p>
    </div>
    <div class="detail-box">
      <h4>Termination</h4>
      <p>When data â‰¤ 8 bytes or single element</p>
    </div>
  </div>
</section>

<style>
  .diagram-section {
    margin: 4rem 0;
    padding: 3rem;
    background: rgba(0, 128, 255, 0.02);
    border-radius: 1rem;
    border: 1px solid rgba(0, 128, 255, 0.1);
  }

  .diagram-section h2 {
    text-align: center;
    margin-bottom: 3rem;
    color: #00d4ff;
  }

  .pipeline {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    margin-bottom: 3rem;
    flex-wrap: wrap;
  }

  .stage {
    text-align: center;
    padding: 2rem;
    background: rgba(255, 255, 255, 0.03);
    border-radius: 1rem;
    border: 1px solid rgba(0, 212, 255, 0.3);
    min-width: 150px;
  }

  .stage-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .stage-label {
    font-weight: 600;
    color: #ffffff;
    margin-bottom: 0.5rem;
  }

  .stage-detail {
    font-size: 0.875rem;
    color: #888888;
    font-family: 'JetBrains Mono', monospace;
  }

  .arrow {
    font-size: 2rem;
    color: #0080ff;
  }

  .diagram-details {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
  }

  .detail-box {
    padding: 1.5rem;
    background: rgba(0, 0, 0, 0.3);
    border-radius: 0.5rem;
    border-left: 3px solid #0080ff;
  }

  .detail-box h4 {
    color: #00d4ff;
    margin-bottom: 0.5rem;
  }

  .detail-box p {
    color: #cccccc;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.875rem;
  }
</style>

