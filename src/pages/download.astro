---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Download ByteLite Blueprint - Free Technical Specification" description="Get ByteLite's technical blueprint revealing 90% of our compression algorithm. Includes bijective pairing, SDD system & implementation guide.">
  <main>
    <section class="download-hero">
      <h1>ByteLite Technical Blueprint</h1>
      <p class="subtitle">Complete implementation guide with ~90% of the algorithm revealed</p>
    </section>

    <div class="download-section">
      <div class="blueprint-preview">
        <h2>What's Included:</h2>
        <ul>
          <li>✓ Complete compression pipeline architecture</li>
          <li>✓ Bijective pairing implementation details</li>
          <li>✓ Segmented Domain Dictionary structure</li>
          <li>✓ Code examples and pseudocode</li>
          <li>✓ Patent-pending algorithm overview</li>
          <li>✓ Performance benchmarks and test results</li>
          <li>✓ Integration guidelines</li>
        </ul>
      </div>
      
      <div class="email-capture">
        <h3>Get Instant Access</h3>
        <form id="blueprint-form">
          <input 
            type="email" 
            name="email" 
            placeholder="Enter your email address" 
            required
          />
          <button type="submit" class="btn btn-primary">
            Download Blueprint
          </button>
        </form>
        <p class="privacy">We respect your privacy. No spam, ever.</p>
      </div>
    </div>
    
    <p class="tagline">200KB engine. Infinite compression. One blueprint.</p>
  </main>
</Layout>

<style>
  main {
    color: white;
    background: #060813;
    padding: 2rem;
    min-height: 100vh;
  }

  .download-hero {
    text-align: center;
    padding: 4rem 2rem 2rem;
  }

  h1 {
    font-size: 3rem;
    background: linear-gradient(135deg, #00D4FF 0%, #FF006E 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 1rem;
  }

  .subtitle {
    font-size: 1.25rem;
    opacity: 0.9;
  }

  .download-section {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    max-width: 1000px;
    margin: 3rem auto;
  }

  .blueprint-preview {
    background: rgba(255, 255, 255, 0.02);
    padding: 2rem;
    border-radius: 12px;
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .blueprint-preview h2 {
    color: #00D4FF;
    margin-bottom: 1.5rem;
  }

  .blueprint-preview ul {
    list-style: none;
    padding: 0;
  }

  .blueprint-preview li {
    padding: 0.75rem 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  }

  .email-capture {
    background: rgba(0, 212, 255, 0.05);
    padding: 2rem;
    border-radius: 12px;
    border: 1px solid rgba(0, 212, 255, 0.2);
    height: fit-content;
  }

  .email-capture h3 {
    color: #00D4FF;
    margin-bottom: 1.5rem;
  }

  form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  input[type="email"] {
    padding: 1rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    color: white;
    font-size: 1rem;
  }

  input[type="email"]:focus {
    outline: none;
    border-color: #00D4FF;
  }

  .btn {
    padding: 1rem 2rem;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
    cursor: pointer;
    border: none;
    font-size: 1rem;
  }

  .btn-primary {
    background: #00D4FF;
    color: #0A0E27;
  }

  .btn-primary:hover {
    background: #00A8CC;
    transform: translateY(-2px);
  }

  .privacy {
    font-size: 0.875rem;
    opacity: 0.7;
    margin-top: 1rem;
  }

  .tagline {
    text-align: center;
    font-size: 1.25rem;
    color: #00D4FF;
    margin-top: 3rem;
  }

  @media (max-width: 768px) {
    .download-section {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  const form = document.getElementById('blueprint-form');
  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    const email = e.target.email.value;
    
    // Here you would normally send to your backend
    // For now, just show success message
    alert('Thank you! The ByteLite Blueprint has been sent to ' + email);
    
    // Clear form
    e.target.reset();
  });
</script>
