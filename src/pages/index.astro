---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import ParadigmShift from '../components/ParadigmShift.astro';
import ProofDemo from '../components/ProofDemo.astro';
import SubscribeForm from '../components/SubscribeForm.astro';
import VerificationSection from '../components/VerificationSection.astro';
---

<Layout
  title="ByteLite - Classical Compression Peaked. This Is What Comes Next."
  description="Patent filed. Hutter Prize submission planned. ByteLite is a dictionary-cascade architecture engineered toward compression ratios on structured data that would leave today's leaders behind. Verification decides how far we actually get."
  keywords={["frontier compression", "dictionary cascade", "Hutter Prize compression", "structured data compression", "deterministic compression", "lossless compression", "ByteLite"]}
  schemaType="Organization"
>
  <Hero />
  
  <!-- Paradigm Shift Section -->
  <ParadigmShift />
  
  <!-- Deep Kore Section -->
  <section class="deep-kore-section py-20">
    <div class="container mx-auto px-4 sm:px-6">
      <div class="max-w-6xl mx-auto">
        <div class="text-center mb-12">
          <h2 class="text-4xl md:text-5xl font-bold mb-6">
            <span class="text-gradient-purple">Deep Kore</span><br/>
            <span class="text-gradient-purple">Beyond Compression. Toward Cognition.</span>
          </h2>
          <p class="text-xl text-gray-300 max-w-3xl mx-auto">
            What if the mathematics of extreme compression were also the mathematics of intelligence?
            Deep Kore is our physics-inspired architecture for finding out. Research in progress. Details restricted.
          </p>
        </div>
        
        <div class="neural-visualization mb-12">
          <div class="neural-container">
            <div class="neural-core"></div>
            <div class="neural-ring ring-1"></div>
            <div class="neural-ring ring-2"></div>
            <div class="neural-ring ring-3"></div>
          </div>
        </div>
        
        <div class="grid md:grid-cols-3 gap-8 mb-12">
          <div class="feature-card purple">
            <div class="feature-icon">🧠</div>
            <h3 class="feature-title">Structure, Not Just Storage</h3>
            <p class="feature-description">
              Compression that might be a blueprint for something bigger
            </p>
          </div>

          <div class="feature-card purple">
            <div class="feature-icon">🔬</div>
            <h3 class="feature-title">Recursive Depth</h3>
            <p class="feature-description">
              How far can you go? We're building to find out.
            </p>
          </div>

          <div class="feature-card purple">
            <div class="feature-icon">⚡</div>
            <h3 class="feature-title">Guardrails Built In</h3>
            <p class="feature-description">
              Genesis Goal Keeper: because "move fast and break things" doesn't apply here
            </p>
          </div>
        </div>
        
        <div class="text-center">
          <a href="/deepkore" class="btn btn-purple btn-large">
            See What's Beyond Compression
          </a>
        </div>
      </div>
    </div>
  </section>
  
  <!-- Proof Demo Section -->
  <ProofDemo />
  
  <!-- Verification Section -->
  <VerificationSection />
  
  <!-- Subscribe Section -->
  <section class="subscribe-section">
    <div class="container">
      <SubscribeForm
        variant="default"
        title="The Waiting List Is Open"
        description="ByteLite early access. Deep Kore research updates. No spam, just milestones."
      />
    </div>
  </section>
</Layout>

<style>
  .subscribe-section {
    padding: 6rem 0;
    background: var(--color-bg-darker);
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }
  
  /* Deep Kore Section */
  .deep-kore-section {
    background: linear-gradient(180deg, transparent 0%, rgba(107, 33, 168, 0.05) 50%, transparent 100%);
    position: relative;
    overflow: hidden;
  }
  
  .text-gradient-purple {
    background: linear-gradient(135deg, #A855F7 0%, #6B21A8 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  
  /* Neural Visualization */
  .neural-visualization {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 300px;
    position: relative;
  }
  
  .neural-container {
    position: relative;
    width: 200px;
    height: 200px;
  }
  
  .neural-core {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 40px;
    height: 40px;
    background: radial-gradient(circle, #A855F7, #6B21A8);
    border-radius: 50%;
    box-shadow: 0 0 60px rgba(168, 85, 247, 0.8);
    animation: pulse 2s infinite;
  }
  
  .neural-ring {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    border: 2px solid rgba(168, 85, 247, 0.3);
    border-radius: 50%;
    animation: rotate 20s linear infinite;
  }
  
  .ring-1 {
    width: 100px;
    height: 100px;
    animation-duration: 10s;
  }
  
  .ring-2 {
    width: 150px;
    height: 150px;
    animation-duration: 15s;
    animation-direction: reverse;
  }
  
  .ring-3 {
    width: 200px;
    height: 200px;
    animation-duration: 20s;
  }
  
  /* Feature Cards */
  .feature-card {
    background: rgba(10, 14, 39, 0.6);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    padding: 2rem;
    text-align: center;
    transition: all 0.3s ease;
  }
  
  .feature-card.purple {
    border-color: rgba(168, 85, 247, 0.3);
  }
  
  .feature-card.purple:hover {
    border-color: rgba(168, 85, 247, 0.5);
    transform: translateY(-5px);
    box-shadow: 0 10px 30px rgba(168, 85, 247, 0.2);
  }
  
  .feature-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }
  
  .feature-title {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: #E0E0E0;
  }
  
  .feature-description {
    color: rgba(224, 224, 224, 0.7);
    font-size: 0.875rem;
  }
  
  /* Buttons */
  .btn {
    padding: 1rem 2.5rem;
    border-radius: 8px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    display: inline-block;
    border: none;
    cursor: pointer;
    font-size: 1rem;
  }
  
  .btn-large {
    padding: 1.25rem 3rem;
    font-size: 1.125rem;
  }
  
  .btn-purple {
    background: linear-gradient(135deg, #A855F7 0%, #6B21A8 100%);
    color: white;
  }
  
  .btn-purple:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 30px rgba(168, 85, 247, 0.4);
  }
  
  @keyframes pulse {
    0% {
      transform: translate(-50%, -50%) scale(1);
      opacity: 1;
    }
    50% {
      transform: translate(-50%, -50%) scale(1.2);
      opacity: 0.8;
    }
    100% {
      transform: translate(-50%, -50%) scale(1);
      opacity: 1;
    }
  }
  
  @keyframes rotate {
    from {
      transform: translate(-50%, -50%) rotate(0deg);
    }
    to {
      transform: translate(-50%, -50%) rotate(360deg);
    }
  }

  @media (max-width: 768px) {
    .subscribe-section {
      padding: 4rem 0;
    }
    
    .neural-visualization {
      height: 200px;
    }
    
    .neural-container {
      transform: scale(0.7);
    }
  }
</style>